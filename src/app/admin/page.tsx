/**
 * Admin dashboard placeholder.
 *
 * This page is gated by AdminLayout (which calls `requireAdmin()`),
 * so only authenticated users can reach it.
 */
export default function AdminPage() {
  return <h1>Admin Dashboard</h1>;
}
